# NOIRÉ

This is a standalone website: everything needed to run it is inside `index.html`.

## Publish on GitHub Pages

1. Upload this folder to a GitHub repository.
2. Open **Settings → Pages**.
3. Set **Source** to **Deploy from a branch**, select `main`, then select `/(root)`.
4. Click **Save** and open the URL GitHub provides.
